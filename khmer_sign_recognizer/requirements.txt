# Computer Vision & Motion Capture
opencv-python==4.13.0.92
mediapipe==0.10.14
rtmlib==0.0.15
onnxruntime-gpu

# Math & Data Processing
# NOTE: If numpy 2.x causes issues with mediapipe, downgrade to numpy==1.26.4
numpy==2.4.4

# Utilities
python-json-logger==4.1.0

# Unicode text rendering on the camera overlay (Khmer + any other language)
pillow>=10.0

# 3D mannequin rendering (scripts/mannequin_local.py)
open3d>=0.18.0

# AI/ML — for local inference (Colab has its own PyTorch)
# Install with CUDA support:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch

# Classical-ML baselines (scripts/run_baseline.py) + parquet dataset import
scikit-learn>=1.3
pandas>=2.0
pyarrow>=14.0